# dependencies
node_modules/

# generated card renders — regenerate via render.mjs.
# keep the showcase deck so the repo shows results.
output/*
!output/ai-domain-expertise/

# pose-extraction intermediates
assets/poses/raw/
assets/poses/_candidates.png

# local Claude Code settings (but keep shared skills in the repo)
.claude/*
!.claude/skills/

# os / editor
*.log
.DS_Store
Thumbs.db

# remotion build artifacts
remotion/out/
remotion/node_modules/
